services:
  bot:
    build: .
    container_name: yandex-pro-assistant
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - FORCE_REINDEX=false
      - KB_PATH=knowledge_base.json
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
